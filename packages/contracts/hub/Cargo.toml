[package]
name = "nirium-hub"
version = "0.1.0"
edition = "2021"
authors = ["Nirium Protocol"]
description = "Nirium Settlement Hub — x402 Micro-billing and MPP Treasury for Soroban"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = "22.0.0"

[dev-dependencies]
soroban-sdk = { version = "22.0.0", features = ["testutils"] }
