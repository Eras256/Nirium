[package]
name = "nirium-sentinel"
version = "0.1.0"
edition = "2021"
authors = ["Nirium Protocol"]
description = "Nirium Neural Sentinel — Agent Performance Scoring Engine for Soroban"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = "22.0.0"

[dev-dependencies]
soroban-sdk = { version = "22.0.0", features = ["testutils"] }
